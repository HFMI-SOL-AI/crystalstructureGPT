[project]
name = "nanogpt"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "torch==2.2.0",
    "numpy",
    "datasets",
    "tiktoken",
    "tqdm",
    "transformers",
    "wandb",
    "pandas>=2.0.3",
]
